server {
    listen 80;
    server_name 52.207.215.136;

    location = /favicon.ico { access_log off; log_not_found off; }
    location /static/ {
        alias /home/ubuntu/postfirerecoverydir/postFireRecovery/static/;
    }
    location / {
        include proxy_params;
        proxy_pass http://unix:/run/gunicorn.sock;
    }

}
